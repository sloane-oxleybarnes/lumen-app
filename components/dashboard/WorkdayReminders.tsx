"use client";

import { useEffect, useState } from "react";

type Reminder = { id: string; title: string; reminder_time: string; days_of_week: number[]; active: boolean };
const dayLabels = ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"];

export default function WorkdayReminders() {
  const [reminders, setReminders] = useState<Reminder[]>([]);
  const [title, setTitle] = useState("Workday check-in");
  const [time, setTime] = useState("09:30");
  const [selectedDays, setSelectedDays] = useState([1, 2, 3, 4, 5]);
  const [saving, setSaving] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const [notificationStatus, setNotificationStatus] = useState<NotificationPermission | "unsupported">("unsupported");

  async function load() { const response = await fetch("/api/workday/reminders"); const data = await response.json().catch(() => null) as { reminders?: Reminder[] } | null; if (response.ok) setReminders(data?.reminders || []); }
  useEffect(() => { void load(); if ("Notification" in window) setNotificationStatus(Notification.permission); }, []);
  async function create(event: React.FormEvent) { event.preventDefault(); setSaving(true); setError(null); try { const response = await fetch("/api/workday/reminders", { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ title, reminder_time: time, days_of_week: selectedDays }) }); const data = await response.json().catch(() => null) as { reminder?: Reminder; error?: string } | null; if (!response.ok || !data?.reminder) throw new Error(data?.error || "Could not save the reminder."); setReminders((current) => [...current, data.reminder!].sort((a, b) => a.reminder_time.localeCompare(b.reminder_time))); } catch (caught) { setError(caught instanceof Error ? caught.message : "Could not save the reminder."); } finally { setSaving(false); } }
  async function toggle(reminder: Reminder) { const response = await fetch(`/api/workday/reminders/${reminder.id}`, { method: "PATCH", headers: { "Content-Type": "application/json" }, body: JSON.stringify({ active: !reminder.active }) }); const data = await response.json().catch(() => null) as { reminder?: Reminder } | null; if (response.ok && data?.reminder) setReminders((current) => current.map((item) => item.id === reminder.id ? data.reminder! : item)); }
  async function remove(id: string) { const response = await fetch(`/api/workday/reminders/${id}`, { method: "DELETE" }); if (response.ok) setReminders((current) => current.filter((item) => item.id !== id)); }
  async function allowNotifications() { if (!("Notification" in window)) return; setNotificationStatus(await Notification.requestPermission()); }

  return <section id="workday-reminders" className="bg-white rounded-card border border-border p-6 mb-5"><h2 className="text-lg text-ink mb-1" style={{ fontFamily: "var(--font-dm-serif), Georgia, serif" }}>Workday reminders</h2><p className="text-sm text-ink-mid">Choose a time for Beckett to prompt you to check in. A reminder never completes a check-in automatically: only you choose what to record. Beckett only shows reminders while it is open and never monitors your activity.</p>{notificationStatus === "default" && <button type="button" onClick={() => void allowNotifications()} className="mt-3 text-sm font-medium text-primary hover:underline">Allow browser notifications while Beckett is open</button>}{notificationStatus === "denied" && <p className="mt-3 text-xs text-ink-light">Browser notifications are off. Your reminders will still appear inside Beckett.</p>}<form onSubmit={create} className="mt-5 rounded-sm border border-border bg-bg/50 p-4"><div className="grid gap-3 sm:grid-cols-2"><label className="text-sm font-medium text-ink">Reminder<input value={title} onChange={(event) => setTitle(event.target.value)} maxLength={120} className="mt-1 block w-full rounded-sm border border-border bg-white px-3 py-2 text-sm font-normal" /></label><label className="text-sm font-medium text-ink">Time<input type="time" value={time} onChange={(event) => setTime(event.target.value)} className="mt-1 block w-full rounded-sm border border-border bg-white px-3 py-2 text-sm font-normal" /></label></div><div className="mt-3 flex flex-wrap gap-2">{dayLabels.map((label, day) => <button key={label} type="button" onClick={() => setSelectedDays((current) => current.includes(day) ? current.filter((item) => item !== day) : [...current, day])} className={`rounded-pill border px-3 py-1 text-xs ${selectedDays.includes(day) ? "border-primary bg-primary-light text-primary" : "border-border bg-white text-ink-mid"}`}>{label}</button>)}</div><button disabled={saving || !title.trim() || !selectedDays.length} className="mt-4 rounded-pill bg-primary px-4 py-2 text-sm font-medium text-white disabled:opacity-60">{saving ? "Saving…" : "Add reminder"}</button>{error && <p className="mt-3 text-sm text-red-700">{error}</p>}</form>{reminders.length > 0 && <div className="mt-5 space-y-3">{reminders.map((reminder) => <div key={reminder.id} className="flex flex-wrap items-center justify-between gap-3 rounded-sm border border-border p-3"><div><p className="text-sm font-medium text-ink">{reminder.title}</p><p className="mt-1 text-xs text-ink-mid">{reminder.reminder_time.slice(0, 5)} · {reminder.days_of_week.map((day) => dayLabels[day]).join(", ")}</p></div><div className="flex gap-3 text-xs font-medium"><button type="button" onClick={() => void toggle(reminder)} className="text-primary hover:underline">{reminder.active ? "Pause" : "Resume"}</button><button type="button" onClick={() => void remove(reminder.id)} className="text-red-700 hover:underline">Delete</button></div></div>)}</div>}</section>;
}
