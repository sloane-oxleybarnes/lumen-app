"use client";

import { useMemo, useState } from "react";

const supports = ["A written agenda or next steps", "A clearer priority or deadline", "A quieter work environment", "A flexible break arrangement", "A modified meeting format", "Something else"];

export default function AccommodationsPage() {
  const [recipient, setRecipient] = useState(""); const [need, setNeed] = useState(""); const [support, setSupport] = useState(supports[0]); const [customSupport, setCustomSupport] = useState(""); const [copied, setCopied] = useState(false);
  const requestedSupport = support === "Something else" ? customSupport.trim() || "a workplace support" : support;
  const draft = useMemo(() => `Subject: Request to discuss workplace support\n\nHi${recipient.trim() ? ` ${recipient.trim()}` : ""},\n\nI would like to discuss a workplace support that would help me do my work effectively.${need.trim() ? ` In particular, ${need.trim()}` : ""}\n\nI am requesting ${requestedSupport}. I would appreciate the chance to talk through options and find an approach that works for the team and my role.\n\nThank you,\n[Your name]`, [recipient, need, requestedSupport]);
  async function copyDraft() { await navigator.clipboard.writeText(draft); setCopied(true); setTimeout(() => setCopied(false), 2000); }
  return <div className="max-w-3xl"><h1 className="mb-2 text-3xl text-ink" style={{ fontFamily: "var(--font-dm-serif), Georgia, serif" }}>Workplace supports</h1><p className="mb-8 text-sm text-ink-mid">Prepare a clear request in your own words. Nothing here is stored or sent by Beckett.</p><div className="mb-6 rounded-sm border border-primary/15 bg-primary-light/40 p-4 text-sm leading-relaxed text-ink-mid">You decide what to disclose. Focus on the support that would help you work effectively; policies, processes, and legal protections vary, so consult your employee handbook, HR, or a qualified local adviser for guidance that applies to you.</div><div className="grid gap-6 lg:grid-cols-2"><section className="rounded-card border border-border bg-white p-6"><h2 className="mb-4 text-lg text-ink" style={{ fontFamily: "var(--font-dm-serif), Georgia, serif" }}>Draft a request</h2><label className="mb-4 block text-sm font-medium text-ink">Recipient <input value={recipient} onChange={(e) => setRecipient(e.target.value)} placeholder="Manager or HR partner" className="mt-1 block w-full rounded-sm border border-border px-3 py-2 text-sm font-normal" /></label><label className="mb-4 block text-sm font-medium text-ink">What would you like to make easier? <textarea value={need} onChange={(e) => setNeed(e.target.value)} maxLength={500} rows={4} placeholder="For example: I can contribute more consistently when I have written next steps after meetings." className="mt-1 block w-full rounded-sm border border-border px-3 py-2 text-sm font-normal" /></label><label className="mb-4 block text-sm font-medium text-ink">Support to request <select value={support} onChange={(e) => setSupport(e.target.value)} className="mt-1 block w-full rounded-sm border border-border bg-white px-3 py-2 text-sm font-normal">{supports.map((item) => <option key={item}>{item}</option>)}</select></label>{support === "Something else" && <input value={customSupport} onChange={(e) => setCustomSupport(e.target.value)} maxLength={250} placeholder="Describe the support" className="mb-4 block w-full rounded-sm border border-border px-3 py-2 text-sm" />}</section><section className="rounded-card border border-border bg-white p-6"><h2 className="mb-4 text-lg text-ink" style={{ fontFamily: "var(--font-dm-serif), Georgia, serif" }}>Your draft</h2><pre className="whitespace-pre-wrap rounded-sm bg-bg p-4 text-sm leading-relaxed text-ink">{draft}</pre><button type="button" onClick={() => void copyDraft()} className="mt-4 rounded-pill bg-primary px-5 py-2 text-sm font-medium text-white hover:bg-primary-dark">{copied ? "Copied" : "Copy draft"}</button></section></div><section className="mt-6 rounded-card border border-border bg-white p-6"><h2 className="text-lg text-ink" style={{ fontFamily: "var(--font-dm-serif), Georgia, serif" }}>Before you send</h2><ul className="mt-3 list-disc space-y-2 pl-5 text-sm leading-relaxed text-ink-mid"><li>Keep the request focused on the support and the work outcome it enables.</li><li>Review your employee handbook or ask HR about the appropriate process.</li><li>Choose what personal information you are comfortable sharing.</li><li>Invite a conversation and consider more than one workable option.</li></ul></section></div>;
}
